spring.application.name=dukanam-backend

# Database Configuration - Uses environment variables in production
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/shopfy?createDatabaseIfNotExist=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SHOW_SQL:false}

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=dukanam.yourlocalstore@gmail.com
spring.mail.password=ubsq lnel biqh faig
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# Server Configuration
server.port=${PORT:8080}

# Logging (reduced for production)
logging.level.org.springframework.security=${LOG_LEVEL:INFO}
logging.level.org.springframework.web=${LOG_LEVEL:INFO}

server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=${INCLUDE_STACKTRACE:never}
